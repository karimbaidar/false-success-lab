# Result grouping update

The scanner result view now shows a priority summary first instead of dumping every finding.

Repeated matches are grouped by action and file. Production findings appear first. Test and frontend matches are hidden behind a Show all control.

The hero headline was also reduced so it stays elegant and does not dominate the first viewport.
